# New features
- [ ] Message logging

# Result types
- [ ] `unwrap` and `expect` to match Rust even more
- [ ] `and_then` and `or_then` methods for chaining calls

# Tables
- [ ] `map`, `merge`, `equals` (shallow) and `filter` methods
- [ ] Different types available for tables (e.g. `list` and `hashmap` instead of being purely abstact)

# Type system enhancements
- [ ] Constructor shortcut 
- [ ] Better support for inheritance
- [ ] `instanceof` for syntatic sugar
